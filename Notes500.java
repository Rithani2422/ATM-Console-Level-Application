// Define a class Notes500 that extends the Notes class
public class Notes500 extends Notes {

    // Constructor for the Notes500 class that takes two parameters: Note and Count
    public Notes500(int Note, int Count) {

        // Call the superclass (Notes) constructor with the provided Note and Count values
        super(Note, Count);
    }
}
