// Define a class Notes200 that extends the Notes class
public class Notes200 extends Notes {

    // Constructor for the Notes200 class that takes two parameters: Note and Count
    public Notes200(int Note, int Count) {

        // Call the superclass (Notes) constructor with the provided Note and Count values
        super(Note, Count);
    }
}
