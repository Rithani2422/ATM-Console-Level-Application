// Define a class Notes100 that extends the Notes class
public class Notes100 extends Notes {

    // Constructor for the Notes100 class that takes two parameters: Note and Count
    public Notes100(int Note, int Count) {

        // Call the superclass (Notes) constructor with the provided Note and Count values
        super(Note, Count);
    }
}
